spring.application.name=backend
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Carpeta donde se guardan los audios
soundmap.upload-dir=uploads

# Tamaño máximo de archivo (ajusta si quieres)
spring.servlet.multipart.max-file-size=20MBspring.application.name=backend

# --- Base de datos ---
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Carpeta donde se guardan los audios ---
soundmap.upload-dir=uploads/audio

# --- Tamaño máximo de archivos ---
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

spring.servlet.multipart.max-request-size=20MB
